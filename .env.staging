# 页面标题
VITE_APP_TITLE = CaseGo

# 生产环境配置
VITE_APP_ENV = 'staging'

# vfadmin管理系统/生产环境
VITE_APP_BASE_API = '/dev-api'

# WebSocket 地址（留空则自动使用当前页面 host，支持 wss）
VITE_APP_WS_URL = ''

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip